Worsheets contain the live editor script to fill to generate the Matlab code, relative to the two assignments (Hopfield model and single perceptron).
Patterns file contain the patterns called in the scripts.
